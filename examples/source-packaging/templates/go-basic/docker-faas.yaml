name: go-basic
runtime: go
command: "./app"
build:
  - "go mod download"
  - "go build -o app ./"
env:
  LOG_LEVEL: info
labels:
  tier: template
secrets:
  - demo-secret
limits:
  memory: "256m"
  cpu: "0.5"
