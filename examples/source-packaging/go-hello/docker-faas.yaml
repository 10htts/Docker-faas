name: hello-go
runtime: go
command: "./app"
build:
  - "go mod download"
  - "go build -o app ./"
env:
  LOG_LEVEL: info
labels:
  tier: demo
secrets:
  - demo-secret
limits:
  memory: "256m"
  cpu: "0.5"
